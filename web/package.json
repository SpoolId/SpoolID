{
  "name": "spoolid-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm --dir ../spec/ts generate && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vite": "^8.1.3"
  },
  "dependencies": {
    "@spoolid/core": "workspace:*"
  }
}
